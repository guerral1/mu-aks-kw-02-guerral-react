import { EXPANDABLE_PANEL_VARIANT } from './constants';
import ExpandablePanel from './ExpandablePanel';

export { EXPANDABLE_PANEL_VARIANT };

export default Object.assign(ExpandablePanel, {
  VARIANT: EXPANDABLE_PANEL_VARIANT,
});
